var socket = require('socket.io-client')();

var SocketClient = function () {
  var _user, _group, _markers;

  /* If marker not exists, create a new one, otherwise, update position */
};

module.exports = SocketClient;
